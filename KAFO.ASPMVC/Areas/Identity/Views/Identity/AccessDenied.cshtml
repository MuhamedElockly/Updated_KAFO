@{
    ViewData["Title"] = "رفض الوصول";
    Layout = "_Layout";
}

@section Styles {
    <style>
        .access-denied-container {
            height: 80vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .access-denied-card {
            border: none;
            border-radius: 1rem;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15);
            overflow: hidden;
            width: 100%;
            max-width: 500px;
            text-align: center;
        }

        .access-denied-header {
            background-color: #e74a3b; /* Red color for error */
            color: white;
            padding: 1.5rem;
            border-bottom: none;
        }

        .access-denied-body {
            padding: 2rem;
            background-color: white;
        }

        .access-denied-icon {
            font-size: 5rem;
            color: #e74a3b;
            margin-bottom: 1.5rem;
        }
    </style>
}

<div class="access-denied-container">
    <div class="access-denied-card">
        <div class="access-denied-header">
            <h3><i class="fas fa-ban me-2"></i>رفض الوصول</h3>
        </div>
        <div class="access-denied-body">
            <div class="access-denied-icon">
                <i class="fas fa-exclamation-circle"></i>
            </div>
            <h4 class="text-danger mb-4">عفواً، ليس لديك صلاحية الوصول إلى هذه الصفحة</h4>

            <p class="mb-4">يبدو أنك تحاول الوصول إلى صفحة لا تمتلك الصلاحيات الكافية للوصول إليها.</p>

            <div class="d-grid gap-2 col-md-8 mx-auto">
                <a asp-controller="Home" asp-action="Index" class="btn btn-primary">
                    <i class="fas fa-home me-2"></i>العودة إلى الصفحة الرئيسية
                </a>
                <a asp-controller="Account" asp-action="Login" class="btn btn-outline-secondary">
                    <i class="fas fa-sign-in-alt me-2"></i>تسجيل الدخول بحساب آخر
                </a>
            </div>
        </div>
    </div>
</div>